# Preregistration Placeholder: Exp10 Attribution Occlusion
- Primary endpoint: delta success after feature ablation.
- Decision: feature is causal if CI lower bound > 0; top-k averaged vector tested vs random control (Holm).
